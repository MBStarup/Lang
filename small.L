    {
        _:;x;{x} # identity function since we can't use parenthesis in expressions #

        a: 0 
        if(a ;;{ # if is imported from the interpreter :) #
                print(3) # print is imported too #
        })
        print(_(4 + 2) * _(1 - 2))
        0
    }